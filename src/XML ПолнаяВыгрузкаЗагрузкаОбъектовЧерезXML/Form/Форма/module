
Процедура КнопкаВыполнитьНажатие(Кнопка)
	
	//{*
	
	пПуть = СокрЛП(ПутьККаталогу);
	Если НЕ (Прав(пПуть,1) = "/" ИЛИ Прав(пПуть,1) = "\") Тогда
		пПуть = пПуть + "\";
	КонецЕсли;
	ИмяфайлаВыгрузки = пПуть + "<Задайте имя файла выгрузки>.xml";
	ИмяфайлаЗагрузки = пПуть + "<Задайте имя файла загрузки>.xml";
	
	//}*
	
	ВыполнитьОбмен(ИмяфайлаВыгрузки,ИмяфайлаЗагрузки);
	
КонецПроцедуры

Процедура ПутьККаталогуНачалоВыбора(Элемент, СтандартнаяОбработка)
	
	ДиалогВыбораФайла = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.ВыборКаталога);
	ДиалогВыбораФайла.Каталог = ПутьККаталогу;
	ДиалогВыбораФайла.МножественныйВыбор = Ложь;
	ДиалогВыбораФайла.Заголовок = "Выбор каталога для обмена";
	
	Если ДиалогВыбораФайла.Выбрать() Тогда
		ПутьККаталогу = ДиалогВыбораФайла.Каталог;
	КонецЕсли;
	
КонецПроцедуры
